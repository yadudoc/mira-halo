#!/bin/bash

DIM_A=4
DIM_B=4
DIM_C=4
DIM_D=4
DIM_E=4
DIM_T=1

for T in $(seq 0 1 $(($DIM_T-1)) )
do
    for E in $(seq 0 1 $(($DIM_E-1)) )
    do
	for D in $(seq 0 1 $(($DIM_D-1)) )
	do
	    for C in $(seq 0 1 $(($DIM_E-1)) )
	    do
		for A in $(seq 0 1 $(($DIM_E-1)) )
		do
		    for A in $(seq 0 1 $(($DIM_E-1)) )
		    do

		    done
		done
	    done
	done
    done
done